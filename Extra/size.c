#include<stdio.h>


int main(){


    // int a;
    // float b;
    // char ch;
    // double d;
    // printf("Size of int = %d\n",sizeof(a));
    // printf("Size of float = %d\n",sizeof(b));
    // printf("Size of char = %d\n",sizeof(ch));
    // printf("Size of double = %d\n",sizeof(d));

    // int a;
    // scanf("%d",&a);
    // printf("ASCII of the number is %c\n",a);

    char ch;
    scanf("%c",&ch);
    // printf("ASCII of the char is: %d\n",ch);
    // printf("ASCII of the char is: %c\n",toupper(ch));
    printf("ASCII of the char is: %c\n",tolower(ch));
    return 0;
}